[project]
name = "lovemeter-backend"
version = "0.1.0"
description = "LoveMeter backend for Vercel deployment"
requires-python = ">=3.12"
dependencies = [
  "fastapi==0.116.1",
  "motor==3.7.1",
  "pydantic==2.11.7",
  "python-dotenv==1.1.1",
  "uvicorn",
]
